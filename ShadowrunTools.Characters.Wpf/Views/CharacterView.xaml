<UserControl x:Class="ShadowrunTools.Characters.Wpf.Views.CharacterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:xtk="http://schemas.xceed.com/wpf/xaml/toolkit"
             xmlns:local="clr-namespace:ShadowrunTools.Characters.Wpf.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <TabControl>
            <TabItem Header="Priorities">
                <local:PrioritiesView DataContext="{Binding Priorities}" />
            </TabItem>
            <TabItem Header="Attributes">
                <local:AttributesView DataContext="{Binding}" />
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
